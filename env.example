# Storage configuration
STORAGE_URL=./storage

# Redis Queue configuration
QUEUE_URL=redis://localhost:6379

# Scene detection threshold (0-100, lower = more sensitive)
SCENE_DETECTION_THRESHOLD=30

# Number of keyframes to extract per scene
KEYFRAMES_PER_SCENE=3

# Output directories
OUTPUT_DIR=./output

